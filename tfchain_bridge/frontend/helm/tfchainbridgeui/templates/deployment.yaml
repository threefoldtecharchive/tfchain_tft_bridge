apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tfchainbridgeui.fullname" . }}
  labels:
    {{- include "tfchainbridgeui.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "tfchainbridgeui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "tfchainbridgeui.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
          - name: REACT_APP_API_URL
            value: {{ .Values.url }}
          - name: REACT_APP_STELLAR_HORIZON_URL
            value: {{ .Values.stellar_horizon_url }}
          - name: REACT_APP_TFT_ASSET_ISSUER
            value: {{ .Values.tft_asset_issuer }}
          - name: REACT_APP_BRIDGE_TFT_ADDRESS
            value: {{ .Values.stellar_bridge_address }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
